# Notebook Update

Explicitly embedded `install_tf.py` code logic into `Agumentation.ipynb` as the first cell block (without using `!pip` specifically, using Python's `subprocess` for safer logging within Jupyter), as requested by the user.
